#!/usr/bin/env bash

function install {
    yarn install
}

function build {
    npx plaintext build src -o out
}

function t {
    echo ""
}

function main {
    for TASK in "$@"
    do
        "$TASK"
    done
    exit
}

main "$@"
