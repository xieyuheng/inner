xvm

- 变长指令集
- register machine -- byte addressed register
- tagged value (64 bits)
- extensible by c

x-forth

- 类 forth 的实现方式，而不是汇编器的实现方式。
  - 方便定义 metadata。
  - 函数内使用 变长指令集。
- stack machine
- tagged value (64 bits)
- extensible by c
